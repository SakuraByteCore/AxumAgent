{
  "name": "pi-task",
  "version": "0.1.0",
  "description": "Task-decomposition slash command for pi-coding-agent — /task prefixes a requirement with a fixed Japanese concurrency template so the main agent splits parallel work into sub-agents and integrates the results, single-file, zero native deps",
  "type": "module",
  "main": "index.ts",
  "files": ["index.ts", "LICENSE"],
  "pi": {
    "extensions": ["./index.ts"]
  },
  "keywords": ["pi-package", "pi", "coding-agent", "extension", "pi-task", "task", "subagents", "parallel", "android", "termux"],
  "license": "MIT",
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.74.0"
  }
}