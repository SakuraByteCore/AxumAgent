{
  "name": "pi-guard",
  "version": "0.1.0",
  "description": "Degradation/loop guard for pi-coding-agent — detects repetitive output at turn boundaries and re-injects a corrective user message plus a continuous system-prompt guard block (single-file, zero native deps)",
  "type": "module",
  "main": "index.ts",
  "files": ["index.ts", "src/detect.ts", "src/prompt.ts"],
  "pi": {
    "extensions": ["./index.ts"]
  },
  "keywords": ["pi-package", "pi", "coding-agent", "extension", "pi-guard", "degradation", "android", "termux"],
  "license": "MIT",
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.74.0"
  }
}
