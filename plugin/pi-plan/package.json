{
  "name": "pi-plan",
  "version": "0.1.0",
  "description": "Plan-first slash command for pi-coding-agent — /plan prefixes a requirement with a fixed English plan-first template so the agent researches and re-confirms the approach before writing any code, single-file, zero native deps",
  "type": "module",
  "main": "index.ts",
  "files": ["index.ts", "LICENSE"],
  "pi": {
    "extensions": ["./index.ts"]
  },
  "keywords": ["pi-package", "pi", "coding-agent", "extension", "pi-plan", "plan", "research", "discuss", "android", "termux"],
  "license": "MIT",
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.74.0"
  }
}
